<%= form_with(model: expense) do |form| %>
  <% if expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
        <% expense.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='my-5'>

  <table class='table'>
    <tbody>
      <tr class='field'>
        <td><%= form.label :date %></td>
        <td><%= form.text_field :date %></td>
      </tr>
      <tr>
        <td><%= form.label :category %></td>
        <td><%= form.text_field :category %></td>
      </tr>
      <tr>
        <td><%= form.label :detail %></td>
        <td><%= form.text_field :detail %></td>
      </tr>
      <tr>
        <td><%= form.label :expense %></td>
        <td><%= form.text_field :expense %></td>
      </tr>
    </tbody>
  </table>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary my-3' %>
  </div>
<% end %>
